Create a ec2 server  (C7I-flex large )
insatll docker and minikube 
install Kubectl  (Its cli tool foe kubernetes)
create a pod
         we can create any resource (pod ,deployment, svc, replication ) in two ways
 1) imperative - over command
 2) declarative - over manifest file 
      /////  POD    //////
It is a smallest object that we can create in K8's.
It is a group of containers.
Pod acts like a single instance for our application.
Pods are ephemeral (short living objects).
Mostly we can use single container inside a pod but if we required, we can create multiple containers inside a same pod.
When we create a pod, containers inside pods can share the same network namespace, and can share the same storage volumes.
While creating pod, we must specify the image, along with any necessary configuration and resource limits.
K8's cannot communicate with containers, they can communicate with only pods.
We can create this pod in two ways:
Imperative (command)
Declarative (Manifest file)

Cluster : Group of servers
pod creates inside cluster 
containers creates inside pod

 1  sudo yum install docker -y
    2  sudo systemctl start docker
    3  clear
    4  curl -LO https://github.com/kubernetes/minikube/releases/latest/download/minikube-linux-amd64
    5  sudo install minikube-linux-amd64 /usr/local/bin/minikube && rm minikube-linux-amd64
    6  minikube start --driver=docker --force
   7  minikube status
   8  curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
   9  chmod +x kubectl
   10  clear
   11  mv kubectl /usr/local/bin/
   12 kubectl version
   13 kubectl run mypod --image=sree978/sree:appv1        -- >to create pod
   14 kubectl get pods                                      --> to check list of pods
   15 kubectl run mypod3 --image=sree978/sree:appv1
   16 kubectl get po                                          --> to check list of pods                             
     we created pods in imperative way meand we did over commands
kubctl api-resources    to get all deatils
    vim pod.yml
  [
---
apiVersion: v1
kind: Pod
metadata:
  name: devops-pod
spec:
  containers:
    - name: mycont
      image: Sree978/sree:appv1
      ports:
        - containerPort: 80
]
    kubectl create -f pod.yml
    kubectl get po
    kubectl get po -o wide     --> to get ip deatils of pod
    kubectl get po -o yaml    --> to get details in yaml format (We wont use it )
    kubectl get po podname -o yaml   --> to get paryicular pod info 
kubctl describe pod podname   --- >to get all info of pod
 kubectl delete pod mypod     ---> to delete pod 
 kubectl delete pod --all   ----> tp delete ll pods
       0r

to get list of pods : kubectl get po
to delete a pod : kubectl delete pod pod-name
to delete all pods : kubectl delete pod --all
to get full info about a pod : kubectl describe pod pod-name
to get pod ip address & node details : kubectl get po -o wide
to execute a manifest file : kubectl create -f filename
imperative way to create a pod : kubectl run pod-name --image=image-name
to see list of resources : kubectl api-resources







 





